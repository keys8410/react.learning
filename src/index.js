import React from 'react';
import ReactDOM from 'react-dom';
import App from './DesafioForms';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);
